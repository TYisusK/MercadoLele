<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Iniciar Sesión</title>
  <link rel="stylesheet" href="/web/css/login.css">
</head>

<body>
  <%- include('templates/header') %>

  <main>
    <div class="login-container">
      <h1 class="login-title">Iniciar Sesión</h1>
      <% if (error) { %>
        <p class="error-message"><%= error %></p>
      <% } %>
      <form action="/iniciar_sesion" method="POST" class="login-form">
        <div class="form-group">
          <label for="username">Usuario</label>
          <input type="text" id="username" name="username" required class="login-input">
        </div>
        <div class="form-group">
          <label for="password">Contraseña</label>
          <input type="password" id="password" name="password" required class="login-input">
        </div>
        

        <div class="form-groupb">
          <button type="submit" class="login-button">Iniciar Sesión</button>
       </div>
      </form>
        


      <p class="login-link">¿No tienes una cuenta? <a href="/registrarse">Registrarse</a></p>
    </div>
  </main>

  <%- include("templates/footer"); %>
</body>

</html>
